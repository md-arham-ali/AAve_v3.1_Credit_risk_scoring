# Direct dependencies only. Transitive deps (jupyter internals, mlflow's Flask/
# alembic/opentelemetry stack, torch's sympy/networkx, lifelines' autograd) are
# resolved by pip — pinning them made this file 69 lines and brittle.
#
# numpy<2 is the binding constraint for the whole file: great_expectations
# 0.18.22 caps numpy<2.0.0, so every other package has to fit under numpy 1.26.4.
# That is what rules out scipy 1.18 (see below). Dropping GE, or moving to GE 1.x,
# is what would unlock numpy 2.

# --- core data ---
pandas==2.3.3
numpy==1.26.4              # capped <2 by great_expectations; was never pinned before

# --- extraction ---
requests==2.33.1
python-dotenv==1.2.2

# --- validation ---
great_expectations==0.18.22

# --- modeling ---
scikit-learn==1.9.0
# NOT 1.18.0 — that requires numpy>=2.0.0, which great_expectations forbids.
# 1.17.1 is the version pip had actually resolved and that was running; the old
# requirements.txt's 1.18.0 pin was stale and had never installed.
scipy==1.17.1
xgboost==3.3.0
lightgbm==4.6.0
lifelines==0.30.3
torch==2.12.1

# --- experiment tracking ---
mlflow==2.22.0             # NEW

# --- Orchestration ---
dagster==1.13.17
dagster-webserver==1.13.17
dagster-dg-cli==1.13.17

# --- plots ---
matplotlib==3.11.0

# --- notebooks / headless execution ---
ipykernel==7.2.0
jupyterlab==4.6.1
notebook==7.6.0
ipywidgets==8.1.8
nbconvert==7.17.1
papermill==2.6.0           # not currently installed locally

# --- dashboard backend ---
fastapi==0.139.2
uvicorn==0.51.0